import 'package:flutter/material.dart';

// กำหนดค่าสีหลัก 
const pColor = Color(0xFF6495ED);
// กำหนดค่าสีรอง
const sColor = Color(0xFFFF7F50);
// กำหนดค่าสีพื้นหลัง
const bColor = Color(0xFF363636);

const sFont = 22.0;